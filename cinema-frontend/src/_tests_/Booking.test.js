import renderer, { act } from 'react-test-renderer';

// import Bookings from '../components/bookings/Bookings';
// import Payment from '../components/bookings/Payment';
// import MovieList from '../components/bookings/MovieList';
// import ShowingsList from '../components/bookings/ShowingsList';
// import Date from '../components/bookings/Date';

// test('Bookings page matches snapshot', () => {
//     const tree = renderer.create(<Bookings />);

//     expect(tree).toMatchSnapshot();
// });

// test('Payment page matches snapshot', () => {
//     const tree = renderer.create(<Payment />);

//     expect(tree).toMatchSnapshot();
// });

// test('Movie list matches snapshot', () => {
//   const tree = renderer.create(<MovieList />);

//   expect(tree).toMatchSnapshot();
// });

// test('Showings list matches snapshot', () => {
//   const tree = renderer.create(<ShowingsList />);

//   expect(tree).toMatchSnapshot();
// });

// test('Date matches snapshot', () => {
//     const tree = renderer.create(<Date />);

//     expect(tree).toMatchSnapshot();
// });

